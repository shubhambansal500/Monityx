<?xml version="1.0" encoding="utf-8"?>
<!--
  Restricts the app to HTTPS-only traffic in release builds.
  The currency API (open.er-api.com) is HTTPS, so no cleartext exceptions needed.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext only in debug builds via debug-overrides -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
